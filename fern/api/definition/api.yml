name: api
auth: api-key
auth-schemes:
  api-key:
    header: X-API-KEY
    name: apiKey
environments:
  Sandbox: https://api.sandbox.primer.io
  Production: https://api.primer.io
default-environment: Production